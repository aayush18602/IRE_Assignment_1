# Heavy deps, only needed on a GPU environment (Kaggle/Colab) for computing
# BERT/XLM-RoBERTa article embeddings at large-bundle scale.
# Install on top of requirements.txt: pip install -r requirements.txt -r requirements-gpu.txt
torch>=2.2
transformers>=4.40
sentencepiece>=0.2  # required by xlm-roberta-base's tokenizer
sentence-transformers>=3.0
faiss-gpu>=1.7; sys_platform == "linux"
